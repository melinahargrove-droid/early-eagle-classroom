# v5.9.79 — Vocabulary Visual Implemented

- The “Jumping pants” definition slide now uses a dedicated visual example instead of the book spread.
- The visual shows a child actively jumping/playing in comfortable pants so the image directly teaches the concept.
- Vocabulary layout is now word + short definition + large visual example.
- Book slides remain book-only and retain the approved large placement.
- The permanent vocabulary visual rule is now implemented in the actual renderer, not just documented.
