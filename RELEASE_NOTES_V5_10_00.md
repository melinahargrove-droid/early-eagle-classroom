# v5.10.00 — Center Choice Queue Layout Refinement

- Fixed the randomized queue controls so they no longer shift sides after the first selection.
- The control bar now has permanent left / center / right slots.
- The child's name is shown by itself.
- `Choose a center!` appears on its own line beneath the name.
- The progress count (`1 of 10 friends`) also appears beneath the name.
- Queue behavior and center capacity logic are unchanged.
