# v4.9 – What’s Next index-sync fix

- Ties the What’s Next illustration directly to the same schedule index used for the highlighted current row.
- Keeps a name-based fallback for custom rows beyond the standard schedule.
- Preserves the centered What’s Next text layout and display-only behavior.
